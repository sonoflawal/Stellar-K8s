# manifest-validator

Simple CLI to validate Kubernetes manifests for StellarNode deployments.

Usage:

```bash
cargo run --manifest-path tools/manifest-validator/Cargo.toml -- tools/manifest-validator/examples/my-manifest.yaml
```
